# department-service
spring:
  application:
    name: department-service
springdoc:
  packageToScan: com.department
# eureka:
#   instance:
#     instance-id: ${spring.application.name}-${random.uuid}
# springdoc:
#   packages-to-scan: com.departementService

